NestJS backend scaffold converted from the original Beego project.

Quick start:

1. cd nest-backend
2. npm install
3. npm run start:dev

API:
- GET /api/ -> info
- GET /api/pembayaran -> list
- POST /api/pembayaran -> create (JSON)
