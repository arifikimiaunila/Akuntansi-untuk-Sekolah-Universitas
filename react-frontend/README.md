Frontend React+TS scaffold for SPP Payment.

Quick start:

1. cd react-frontend
2. npm install
3. npm run dev

Note: dev server proxies `/api` to backend if configured. For quick testing run both backend and frontend on same host and let frontend call `/api/...`.
